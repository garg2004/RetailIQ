# Copy this file to .env and fill in your local PostgreSQL credentials.
# Only needed if DEMO_MODE=false (i.e. you're running against real Postgres
# instead of the built-in SQLite demo database).

DEMO_MODE=true

DB_HOST=localhost
DB_PORT=5432
DB_NAME=retailiq
DB_USER=postgres
DB_PASSWORD=your_password_here
